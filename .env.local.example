# Resend email — required to capture early-access signups in production.
# Get a key at https://resend.com (free tier sends from onboarding@resend.dev).
RESEND_API_KEY=
# Where signup notifications are delivered (your inbox).
CONTACT_EMAIL=
# Optional: verified sender. Defaults to "Voice.ai Phone <onboarding@resend.dev>".
RESEND_FROM=
